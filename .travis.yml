language: php
php: [5.6]
env:
  global:
    - MYV=${TRAVIS_BRANCH}
    - FILE=build-${TRAVIS_BRANCH}-${TRAVIS_TAG}.tar.gz

install:
  - source vars.bash

script:
  - pwd
  - echo ${FOO} ${BAR} ${MYV} ${TRAVIS_TAG}
  - echo ${FILE/-./.}
  - which node
  - node --version
  - which npm
  - which gem
  - which ruby
